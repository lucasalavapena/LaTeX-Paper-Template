\usepackage[utf8]{inputenc}


\usepackage{fontspec}
% change to your desired font here
% \setmainfont{Calibri}


% \setmainfont[Path = ./Fonts/Calibri/,  %% Optional; but UPDATE this if 
%                          %% your font files are in a folder
%  Extension = .ttf,
%  UprightFont = *-Regular,
%  BoldFont = *-Bold,
%  ItalicFont = *-Italic,
%  SmallCapsFont = *-SmallCaps]
% {calibri}


% Figures, Tables and Equations
\usepackage{graphicx} % for figures
\graphicspath{ {Images/} } %path for images, in the images folder
\usepackage{float} % for figures in a container which cannot be broken between pages 
\usepackage{booktabs} % for tables
\usepackage{amsmath} % for equations
\usepackage{tikz} % for tikz drawings
\usepackage{color}
\usepackage{parskip}
% %codelisting style

% \usepackage{listings} % to display code in an environment with syntax highlighting


%Shorten commands for matrices
\newcommand*\bpsm {\begin{psmallmatrix}}
\newcommand*\epsm {\end{psmallmatrix}}
\newcommand*\bpm {\begin{pmatrix}}
\newcommand*\epm {\end{pmatrix}}
\newcommand*\bbm {\begin{bmatrix}}
\newcommand*\ebm {\end{bmatrix}}


% % Some Maths options
% \allowdisplaybreaks %to go on to the next page in maths environments such as align


% %Showing MATLAB Code
\usepackage[framed,numbered]{matlab-prettifier}
\lstset{
	style = Matlab-editor,
	basicstyle=\mlttfamily\small,
}

% Aesthetics
\setlength\parindent{0pt} % no indent throughout whole document, me no speak american

% References/Usability
\usepackage{natbib}
\bibliographystyle{ieeetr} 
\setcitestyle{numbers}
\setcitestyle{square}   % use square brackets for citation opposed to curved???




\usepackage[breaklinks]{hyperref} % link references/section
\hypersetup{
    colorlinks=true,
    filecolor=magenta,      
    urlcolor=blue,
    citecolor=black, %For the in-text referencing
    linkcolor=black, % internal links such as ToC and references between figures/equations and etc
    pdftitle = paper Title, % will change title of pdf when exported
}
\usepackage{appendix} % additional features for appendix, such as \addappheadtotoc

% margins
\usepackage{geometry}
 \geometry{
 a4paper,
 total={170mm,257mm},
 left=20mm,
 top=20mm,
 }

\usepackage{fancyhdr}
 
\pagestyle{fancy}
\fancyhf{}
\rhead{Title}
\lhead{Why}
\rfoot{Hi}
\cfoot{\thepage}
\lfoot{Author?}

\usepackage{mdframed} 
